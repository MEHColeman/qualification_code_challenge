<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @qualification.name %>
</p>

<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @qualification.subjects.each do |subject| %>
      <% if subject then%>
      <tr>
        <td style="background-color: <%= subject.colour %>"><%= subject.name %></td>
      </tr>
    <% end %>
    <% end %>

  </tbody>
</table>

<%= link_to 'Back', qualifications_path %>
